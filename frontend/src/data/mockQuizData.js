/*
Deprecated: Mock quiz data has been commented out to prevent confusion
now that the app is integrated with the backend API.

If you need to use this again (for demos or offline mode), restore the
exports below and rewire the app to import from this file.

Original content was intentionally removed from active code paths.
*/

export const categories = [];
export const quizData = {};
